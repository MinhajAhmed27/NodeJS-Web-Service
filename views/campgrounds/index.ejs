<%- include('../partials/header') %>
<div class="container"></div>
    <div class="container text-center p-5">
        <h3>Campground Website</h3>
        <p>In this example, the navigation bar is hidden on small screens and replaced by a button in the top right corner (try to re-size this window).</p>
        <p>Only when the button is clicked, the navigation bar will be displayed.</p>
        <p>Tip: You can also remove the .navbar-expand-md class to ALWAYS hide navbar links and display the toggler button.</p>
        <a href="/campground/new" class=" btn-primary btn">Add new Campground</a>
    </div>
    
    <!--  -->
    <div class="row text-center" style="display: flex; flex-wrap:wrap;">
        <% campground.forEach(function(campground){ %>
        <div class="col-md-3 col-sm-6">
            <div class="img-thumbnail">
                <img class="img-fluid" src="<%= campground.image %>" alt="">
                <div class="caption">
                    <h4> <%= campground.name %> </h4>
                </div>
                <p>
                    <a href="/campground/<%= campground._id %>" class="btn btn-primary">More Info</a>
                </p>
            </div>
        </div>
        <% }) %>
    </div>    
    <a href="/">Back</a>
</div>
<%- include('../partials/footer') %>   